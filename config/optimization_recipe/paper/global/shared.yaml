# @package _global_
defaults:
  - /experiment: paper/global/shared
  - /bayes_opt: base_opt
  - _self_
optimization_recipe:
  name: paper/global/shared
  mode: shared
  matrices: [q, r]
  seed: 42
  initial_random_points: 5
  evaluations_per_parameter: 10
  tracking_processes: 0
  ledger: ${path_join:${environment_cfg.logging_base_path},optimization,global_shared.jsonl}
  best_result: ${path_join:${environment_cfg.logging_base_path},optimization,global_shared_best.json}
