# @package _global_
defaults:
  - override /detector_cfg: centerpoint
  - override /model_cfg/matrix_q: centerpoint/global/val/shared_opt
  - override /model_cfg/matrix_r: centerpoint/global/val/shared_opt
  - override /poly_mot: nusc_config
  - _self_

experiment_id: paper/global/shared
eval_only: False
dataset_cfg:
  split: val
eval_path: ${path_join:${environment_cfg.logging_base_path},paper,global,shared,eval}
result_path: ${path_join:${environment_cfg.logging_base_path},paper,global,shared,result}
